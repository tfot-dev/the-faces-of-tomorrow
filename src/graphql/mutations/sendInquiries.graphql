#import "../fragments/inquiry.graphql"

mutation sendInquiries($name: String!, $email: String!, $query: String!) {
    sendInquiries(objects: [{name: $name, email: $email, query: $query}]) {
        returning {...Inquiry}
    }
}